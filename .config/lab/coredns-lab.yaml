# In-cluster CoreDNS secondary for the lab.local zone.
# PRIMARY/master is the Synology DS918+ DNS Server (192.168.50.144 — confirmed
# via SOA; .102 is a stale NAS IP and serves no DNS).
corednsLab:
  transferFrom: 192.168.50.144   # DS918 DNS master — allows AXFR from 192.168.50.0/24
  loadBalancerIP: 192.168.50.180 # freed from the old argocd LoadBalancer
