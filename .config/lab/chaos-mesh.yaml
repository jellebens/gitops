chaosDaemon:
  runtime: containerd
  socketPath: /run/k3s/containerd/containerd.sock

dashboard:
  securityMode: false
  ingress:
    enabled: false

controllerManager:
  leaderElection:
    enabled: true