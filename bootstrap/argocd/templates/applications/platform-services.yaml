{{- if .Values.applications.platformServices.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: platform-services
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: {{ .Values.spec.source.repoURL | quote }}
        revision: {{ .Values.spec.source.targetRevision | quote }}
        directories:
          - path: platformservices/*
  template:
    metadata:
      name: 'platform-{{`{{ .path.basenameNormalized }}`}}'
    spec:
      project: platform-services
      source:
        repoURL: {{ .Values.spec.source.repoURL | quote }}
        targetRevision: {{ .Values.spec.source.targetRevision | quote }}
        path: '{{`{{ .path.path }}`}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{`{{ .path.basenameNormalized }}`}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
{{- end }}