apiVersion: v1
kind: Secret
metadata:
  name: cert-manager-repo
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-400"
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  name: cert-manager-repo
  project: platform-services
  url: {{ .Values.repos.certManager.url | quote }}
  type: helm
