gateway:
  name: cilium-gateway
  namespace: gateway
  className: cilium
  listenerName: http
  listenerPort: 80
  https:
    enabled: false
    listenerName: https
    listenerPort: 443
    hostname: ""
    tls:
      mode: Terminate
      certificateRefs: []
  # Additional HTTPS listeners on the same Gateway, differentiated by SNI.
  # Each entry: name, [port], hostname, tls.{mode, certificateRefs[]}
  additionalHttpsListeners: []
  annotations: {}

# List of HTTPRoutes to provision against the shared Gateway.
# Each entry: name, namespace, hostnames[], backend.{name,port}, [path]
routes: []

# HTTP -> HTTPS 302 redirects on the shared plain `http` listener, for
# services whose content route is HTTPS-only. Each entry: name, namespace,
# hostnames[]. See templates/http-redirects.yaml.
httpRedirects: []
