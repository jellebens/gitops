apiVersion: v2
name: pomona
# version: bump on EVERY release that touches this chart (owner rule 2026-08-31):
# patch for fixes/docs/reports, minor for features. 0.2.1 = trued-up baseline
# (0.1.0 shipped twice unbumped: #222 feature -> 0.2.0, #261 fix -> 0.2.1).
# 0.3.0 = #278 Demeter autodosing controller (feature; ships in shadow mode).
# 0.3.1 = #278 Demeter ACTIVE in lab + dosing dashboard row/annotations.
# 0.4.0 = #278 Demeter 0.2.0 self-learning brain.
# 0.4.1 = #278 dashboard row renamed "Demeter", dose markers only on pH/EC;
#         source pointers -> github.com/jellebens/demeter.
# 0.5.0 = #278 Demeter 0.3.0 — config describes the tower (ADR-0002).
# 0.5.1 = #278 "Last dose" stat survives controller restarts (7 d lookback).
# 0.6.0 = #278 Demeter 0.4.0 — mixes what it doses (pump override, ADR-0003),
#         tier-scoped early release, late responses learned.
# 0.6.1 = #278 dashboard: reagent bottle + mixing/pump panels.
# 0.6.2 = #278 baseline 2026-09-11: prime_ml 0 (lines flushed, carry reagent);
#         Mixing & pump panel shows the Fibaro pump POWER lane (HA/Influx).
# 0.6.3 = #278 Demeter 0.4.1 — daily caps as rails; lab removes both caps.
# 0.7.0 = #278 Demeter 0.5.0 — learned dosing-rate baseline (ADR-0004) +
#         "Dosing vs usual" panel.
# 0.7.1 = #278 Demeter 0.5.1 — the pump is confirmed by plug power before a
#         dose is judged (ADR-0005); homeassistant publishes pomona/pump/power.
# 0.8.0 = #291 Demeter retired from this zone (demeter.enabled: false): the tower's brain
#         now runs as demeter-brain-pomona-0001 in landingzones/demeter (cutover 2026-09-12).
# 0.8.1 = ceres #301: archive pomona/pump/request|reason|override as strings (pomona_meta) — the
#         evidence the 2026-09-13 04:13Z re-mix incident lacked.
version: 0.8.3
type: application
appVersion: "1.0.0"
description: Pomona hydroponic tower landing zone — MQTT -> InfluxDB telemetry ingestion (Telegraf), Grafana dashboard
