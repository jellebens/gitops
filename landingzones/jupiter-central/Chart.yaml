apiVersion: v2
name: jupiter-central
version: 0.1.0
type: application
appVersion: "0.1.0"
description: >-
  Jupiter central services landing zone (price-service, forecast-service + its
  training CronJob, and the reporting-service fleet-reporting/savings consumer).
  One instance serves the whole fleet — central-only, touches nothing zeus does.
