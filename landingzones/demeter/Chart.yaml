apiVersion: v2
name: demeter
# version: bump on EVERY release that touches this chart (owner rule 2026-08-31):
# patch for fixes/docs/reports, minor for features.
# 0.1.0 = #291 first landing zone: one demeter-brain Deployment per unit
#         (pomona-0001 in shadow, soaking next to the live pomona-demeter).
# 0.2.0 = #291 cutover (2026-09-12, soak skipped by the owner): ownPrefix demeter — the
#         brain inherits the live retained ledger; pomona-0001 goes ACTIVE in lab.
# 0.3.0 = #292 slice 2: the config database (CloudNativePG Cluster demeter-pg) + the
#         registry service; brains can read sys/config from MQTT (configSource).
version: 0.3.0
type: application
appVersion: "0.7.0"
description: Demeter landing zone — one self-learning brain per hydroponic unit (demeter ADR-0007/0010), rendered from the units map
