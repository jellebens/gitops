{{- if .Values.telegraf.enabled }}
# demeter-telegraf (demeter architecture §8 R1, ADR-0006, card #295): the
# long-term archive of every unit's v2 tree, MQTT demeter/# -> InfluxDB bucket
# `demeter`. PURE CONSUMER: it subscribes and writes time series, publishes
# nothing, actuates nothing. Every series carries the `unit` tag so training
# data stays separable per reservoir:
#   unit_tele{unit,zone,metric}         tele/<zone>/<metric>            float
#   unit_actuator{unit,actuator,metric} actuator/<name>/power_w         float
#   unit_meta{unit,metric|actuator}     sys/status|meta|health|role, actuator/<name>/state|reason   string
#   unit_docs{unit,doc}                 sys/decision|ledger|alerts|advice|config, dose/result, desired  string (JSON verbatim)
#   demeter_sys{metric|service}         demeter/sys/mode|alerts|units, demeter/sys/status/<service>    string
# The pre-cutover tower history stays in the `pomona` bucket (landingzones/pomona).
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: demeter-telegraf
  namespace: demeter
  labels:
    app.kubernetes.io/name: demeter-telegraf
    app.kubernetes.io/part-of: demeter
data:
  telegraf.conf: |
    [agent]
      interval = "30s"
      round_interval = true
      flush_interval = "30s"
      metric_batch_size = 1000
      metric_buffer_limit = 10000
      omit_hostname = true

    [[outputs.influxdb_v2]]
      urls = ["{{ .Values.telegraf.influx.url }}"]
      token = "${INFLUX_TOKEN}"
      organization = "{{ .Values.telegraf.influx.org }}"
      bucket = "{{ .Values.telegraf.influx.bucket }}"

    [[inputs.internal]]
      collect_memstats = false

    [[outputs.prometheus_client]]
      listen = ":{{ .Values.telegraf.metricsPort }}"
      metric_version = 2
      expiration_interval = "5m"

    # Numeric telemetry: one number per topic (ADR-0008 rule 1).
    [[inputs.mqtt_consumer]]
      servers = ["tcp://{{ .Values.telegraf.mqtt.host }}:{{ .Values.telegraf.mqtt.port }}"]
      client_id = "{{ .Values.telegraf.mqtt.clientId }}"
      username = "${MQTT_USER}"
      password = "${MQTT_PASS}"
      qos = {{ .Values.telegraf.mqtt.qos }}
      persistent_session = false
      topics = ["demeter/+/tele/+/+"]
      data_format = "value"
      data_type = "float"
      name_override = "unit_tele"
      [[inputs.mqtt_consumer.topic_parsing]]
        topic = "demeter/+/tele/+/+"
        tags = "_/unit/_/zone/metric"

    # The mixing evidence: watts per actuator (Home Assistant publishes them).
    [[inputs.mqtt_consumer]]
      servers = ["tcp://{{ .Values.telegraf.mqtt.host }}:{{ .Values.telegraf.mqtt.port }}"]
      client_id = "{{ .Values.telegraf.mqtt.clientId }}-actuator"
      username = "${MQTT_USER}"
      password = "${MQTT_PASS}"
      qos = {{ .Values.telegraf.mqtt.qos }}
      persistent_session = false
      topics = ["demeter/+/actuator/+/power_w"]
      data_format = "value"
      data_type = "float"
      name_override = "unit_actuator"
      [[inputs.mqtt_consumer.topic_parsing]]
        topic = "demeter/+/actuator/+/+"
        tags = "_/unit/_/actuator/metric"

    # Retained state, as strings.
    [[inputs.mqtt_consumer]]
      servers = ["tcp://{{ .Values.telegraf.mqtt.host }}:{{ .Values.telegraf.mqtt.port }}"]
      client_id = "{{ .Values.telegraf.mqtt.clientId }}-meta"
      username = "${MQTT_USER}"
      password = "${MQTT_PASS}"
      qos = {{ .Values.telegraf.mqtt.qos }}
      persistent_session = false
      topics = [
        "demeter/+/sys/status",
        "demeter/+/sys/meta",
        "demeter/+/sys/health",
        "demeter/+/sys/role",
        "demeter/+/actuator/+/state",
        "demeter/+/actuator/+/reason",
      ]
      data_format = "value"
      data_type = "string"
      name_override = "unit_meta"
      [[inputs.mqtt_consumer.topic_parsing]]
        topic = "demeter/+/sys/+"
        tags = "_/unit/_/metric"
      [[inputs.mqtt_consumer.topic_parsing]]
        topic = "demeter/+/actuator/+/+"
        tags = "_/unit/_/actuator/metric"

    # The documents, verbatim JSON — the brain's decisions and ledger, Robigus's
    # alerts and advice, the registry's config and desired state, dose acks.
    # Forever (ADR-0006): the model-training corpus.
    [[inputs.mqtt_consumer]]
      servers = ["tcp://{{ .Values.telegraf.mqtt.host }}:{{ .Values.telegraf.mqtt.port }}"]
      client_id = "{{ .Values.telegraf.mqtt.clientId }}-docs"
      username = "${MQTT_USER}"
      password = "${MQTT_PASS}"
      qos = {{ .Values.telegraf.mqtt.qos }}
      persistent_session = false
      topics = [
        "demeter/+/sys/decision",
        "demeter/+/sys/ledger",
        "demeter/+/sys/alerts",
        "demeter/+/sys/advice",
        "demeter/+/sys/config",
        "demeter/+/dose/result",
        "demeter/+/desired",
      ]
      data_format = "value"
      data_type = "string"
      name_override = "unit_docs"
      [[inputs.mqtt_consumer.topic_parsing]]
        topic = "demeter/+/sys/+"
        tags = "_/unit/_/doc"
      [[inputs.mqtt_consumer.topic_parsing]]
        topic = "demeter/+/dose/+"
        tags = "_/unit/_/doc"
      [[inputs.mqtt_consumer.topic_parsing]]
        topic = "demeter/+/desired"
        tags = "_/unit/doc"

    # Demeter's own system layer.
    [[inputs.mqtt_consumer]]
      servers = ["tcp://{{ .Values.telegraf.mqtt.host }}:{{ .Values.telegraf.mqtt.port }}"]
      client_id = "{{ .Values.telegraf.mqtt.clientId }}-sys"
      username = "${MQTT_USER}"
      password = "${MQTT_PASS}"
      qos = {{ .Values.telegraf.mqtt.qos }}
      persistent_session = false
      topics = [
        "demeter/sys/mode",
        "demeter/sys/alerts",
        "demeter/sys/advice",
        "demeter/sys/units",
        "demeter/sys/status/+",
      ]
      data_format = "value"
      data_type = "string"
      name_override = "demeter_sys"
      [[inputs.mqtt_consumer.topic_parsing]]
        topic = "demeter/sys/+"
        tags = "_/_/metric"
      [[inputs.mqtt_consumer.topic_parsing]]
        topic = "demeter/sys/status/+"
        tags = "_/_/metric/service"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: demeter-telegraf
  namespace: demeter
  labels:
    app.kubernetes.io/name: demeter-telegraf
    app.kubernetes.io/part-of: demeter
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: demeter-telegraf
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: demeter-telegraf
        app.kubernetes.io/part-of: demeter
      annotations:
        checksum/config: {{ toYaml .Values.telegraf | sha256sum }}
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: {{ .Values.runtime.uid }}
        runAsGroup: {{ .Values.runtime.gid }}
        fsGroup: {{ .Values.runtime.gid }}
      containers:
        - name: telegraf
          image: "{{ .Values.telegraf.image.repository }}:{{ .Values.telegraf.image.tag }}"
          imagePullPolicy: {{ .Values.telegraf.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          # MQTT_USER / MQTT_PASS (the telegraf-demeter broker user) and
          # INFLUX_TOKEN (a write token scoped to the demeter bucket). optional:
          # until sealed the pod runs and nothing flows (fail-and-retry).
          envFrom:
            - secretRef:
                name: {{ .Values.telegraf.secret.name }}
                optional: true
          ports:
            - name: metrics
              containerPort: {{ .Values.telegraf.metricsPort }}
              protocol: TCP
          readinessProbe:
            httpGet: { path: /metrics, port: metrics }
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            httpGet: { path: /metrics, port: metrics }
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 6
          volumeMounts:
            - name: config
              mountPath: /etc/telegraf/telegraf.conf
              subPath: telegraf.conf
              readOnly: true
          resources:
            {{- toYaml .Values.telegraf.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: demeter-telegraf
---
apiVersion: v1
kind: Service
metadata:
  name: demeter-telegraf-metrics
  namespace: demeter
  labels:
    app.kubernetes.io/name: demeter-telegraf
    app.kubernetes.io/part-of: demeter
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: demeter-telegraf
  ports:
    - name: metrics
      port: {{ .Values.telegraf.metricsPort }}
      targetPort: metrics
{{- if .Values.serviceMonitor.enabled }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: demeter-telegraf
  namespace: demeter
  labels:
    app.kubernetes.io/name: demeter-telegraf
    app.kubernetes.io/part-of: demeter
    release: {{ .Values.serviceMonitor.releaseLabel | quote }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: demeter-telegraf
  namespaceSelector:
    matchNames: [demeter]
  endpoints:
    - port: metrics
      path: /metrics
      interval: {{ .Values.serviceMonitor.interval }}
{{- end }}
{{- if .Values.networkPolicy.enabled }}
---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: demeter-telegraf
  namespace: demeter
  labels:
    app.kubernetes.io/name: demeter-telegraf
    app.kubernetes.io/part-of: demeter
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: demeter-telegraf
  ingress:
    - fromEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: {{ .Values.networkPolicy.metricsScrapeNamespace | quote }}
      toPorts:
        - ports:
            - port: {{ .Values.telegraf.metricsPort | quote }}
              protocol: TCP
{{- end }}
{{- if and .Values.telegraf.secret.sealedSecret.enabled .Values.telegraf.secret.sealedSecret.encryptedData }}
---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: {{ .Values.telegraf.secret.name }}
  namespace: demeter
spec:
  encryptedData:
    {{- range $k, $v := .Values.telegraf.secret.sealedSecret.encryptedData }}
    {{- if $v }}
    {{ $k }}: {{ $v | quote }}
    {{- end }}
    {{- end }}
  template:
    metadata:
      name: {{ .Values.telegraf.secret.name }}
      namespace: demeter
    type: Opaque
{{- end }}
{{- end }}
