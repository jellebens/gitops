{{- if .Values.registry.enabled }}
# demeter-registry (ADR-0010/0011, card #292): the only writer of the config
# database and the only publisher of every unit's retained
# demeter/<unit>/sys/config + desired, demeter/sys/units and demeter/sys/mode.
# Brains read MQTT only; a registry outage freezes config at its last
# projection and changes nothing else. Single replica + Recreate: one writer.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: demeter-registry
  namespace: demeter
  labels:
    app.kubernetes.io/name: demeter-registry
    app.kubernetes.io/part-of: demeter
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: demeter-registry
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: demeter-registry
        app.kubernetes.io/part-of: demeter
      annotations:
        checksum/config: {{ printf "%s|%s" .Values.mode (toYaml .Values.registry) | sha256sum }}
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: {{ .Values.runtime.uid }}
        runAsGroup: {{ .Values.runtime.gid }}
        fsGroup: {{ .Values.runtime.gid }}
      containers:
        - name: registry
          image: "{{ .Values.registry.image.repository }}:{{ .Values.registry.image.tag }}"
          imagePullPolicy: {{ .Values.registry.image.pullPolicy }}
          args: ["serve"]
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          env:
            - name: DEMETER_FLEET_MODE
              value: {{ .Values.mode | quote }}
            - name: MQTT_HOST
              value: {{ .Values.registry.mqtt.host | quote }}
            - name: MQTT_PORT
              value: {{ .Values.registry.mqtt.port | quote }}
            - name: MQTT_CLIENT_ID
              value: {{ .Values.registry.mqtt.clientId | quote }}
            - name: REGISTRY_PORT
              value: {{ .Values.registry.apiPort | quote }}
            - name: METRICS_PORT
              value: {{ .Values.metrics.port | quote }}
            - name: REPUBLISH_SECONDS
              value: {{ .Values.registry.republishSeconds | quote }}
            # the CloudNativePG app secret: PGHOST/PGPORT/PGDATABASE/PGUSER/PGPASSWORD
            - name: PGHOST
              valueFrom: { secretKeyRef: { name: {{ .Values.postgres.name }}-app, key: host } }
            - name: PGPORT
              valueFrom: { secretKeyRef: { name: {{ .Values.postgres.name }}-app, key: port } }
            - name: PGDATABASE
              valueFrom: { secretKeyRef: { name: {{ .Values.postgres.name }}-app, key: dbname } }
            - name: PGUSER
              valueFrom: { secretKeyRef: { name: {{ .Values.postgres.name }}-app, key: username } }
            - name: PGPASSWORD
              valueFrom: { secretKeyRef: { name: {{ .Values.postgres.name }}-app, key: password } }
          # MQTT_USER / MQTT_PASS (the `registry` broker user) + REGISTRY_TOKEN
          # (API writes). optional: until sealed the registry still serves reads
          # and projects nothing (it cannot connect) — brains keep their last
          # retained document.
          envFrom:
            - secretRef:
                name: {{ .Values.registry.secret.name }}
                optional: true
          ports:
            - name: api
              containerPort: {{ .Values.registry.apiPort }}
              protocol: TCP
            {{- if .Values.metrics.enabled }}
            - name: metrics
              containerPort: {{ .Values.metrics.port }}
              protocol: TCP
            {{- end }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: api
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: api
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 6
          resources:
            {{- toYaml .Values.registry.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: demeter-registry
  namespace: demeter
  labels:
    app.kubernetes.io/name: demeter-registry
    app.kubernetes.io/part-of: demeter
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: demeter-registry
  ports:
    - name: api
      port: {{ .Values.registry.apiPort }}
      targetPort: api
    {{- if .Values.metrics.enabled }}
    - name: metrics
      port: {{ .Values.metrics.port }}
      targetPort: metrics
    {{- end }}
{{- if and .Values.metrics.enabled .Values.serviceMonitor.enabled }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: demeter-registry
  namespace: demeter
  labels:
    app.kubernetes.io/name: demeter-registry
    app.kubernetes.io/part-of: demeter
    release: {{ .Values.serviceMonitor.releaseLabel | quote }}
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: demeter-registry
  namespaceSelector:
    matchNames: [demeter]
  endpoints:
    - port: metrics
      path: /metrics
      interval: {{ .Values.serviceMonitor.interval }}
{{- end }}
{{- if .Values.networkPolicy.enabled }}
---
# Ingress-only policy for the registry: the Prometheus scrape, and the API
# from inside the cluster (Home Assistant reaches it through the gateway in a
# later card; the advisors run in-cluster). Egress stays open (broker, DB).
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: demeter-registry
  namespace: demeter
  labels:
    app.kubernetes.io/name: demeter-registry
    app.kubernetes.io/part-of: demeter
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: demeter-registry
  ingress:
    - fromEntities: [cluster]
      toPorts:
        - ports:
            - port: {{ .Values.registry.apiPort | quote }}
              protocol: TCP
    {{- if .Values.metrics.enabled }}
    - fromEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: {{ .Values.networkPolicy.metricsScrapeNamespace | quote }}
      toPorts:
        - ports:
            - port: {{ .Values.metrics.port | quote }}
              protocol: TCP
    {{- end }}
{{- end }}
{{- if and .Values.registry.secret.sealedSecret.enabled .Values.registry.secret.sealedSecret.encryptedData }}
---
# MQTT_USER / MQTT_PASS of the `registry` broker user + REGISTRY_TOKEN.
# Seal for ns demeter / secret {{ .Values.registry.secret.name }}.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: {{ .Values.registry.secret.name }}
  namespace: demeter
spec:
  encryptedData:
    {{- range $k, $v := .Values.registry.secret.sealedSecret.encryptedData }}
    {{- if $v }}
    {{ $k }}: {{ $v | quote }}
    {{- end }}
    {{- end }}
  template:
    metadata:
      name: {{ .Values.registry.secret.name }}
      namespace: demeter
    type: Opaque
{{- end }}
{{- end }}
