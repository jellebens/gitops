apiVersion: v2
name: applications
description: A Helm chart for Kubernetes

# A chart can be either an 'application' or a 'library' chart.
#
# Application charts are a collection of templates that can be packaged into versioned archives
# to be deployed.
#
# Library charts provide useful utilities or functions for the chart developer. They're included as
# a dependency of application charts to inject those utilities and functions into the rendering
# pipeline. Library charts do not define any templates and therefore cannot be deployed.
type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
# 0.1.1 = ceres + kyverno-config opt into server-side diff (compare-options
#         ServerSideDiff=true) — ends the phantom-diff self-heal loop.
# 0.1.2 = kyverno too: its policies.kyverno.io CRDs showed the same phantom diff
#         once the SyncError condition was cleared.
# 0.1.3 = ceres #295 step 5: the `pomona` application (landingzones/pomona — the v1 Telegraf
#         ingestion bridge and the Pomona history board) is gone; the tower speaks
#         ceres/pomona-0001/# and is archived by landingzones/ceres. The `pomona` bucket stays.
version: 0.1.3

# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application. Versions are not expected to
# follow Semantic Versioning. They should reflect the version the application is using.
# It is recommended to use it with quotes.
appVersion: "1.16.0"
